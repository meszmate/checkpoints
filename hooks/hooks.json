{
  "description": "Checkpoints plugin hooks",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "command": "sh ${CLAUDE_PLUGIN_ROOT}/hooks/strip-attribution.sh"
      }
    ]
  }
}
