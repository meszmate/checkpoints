{
  "description": "Checkpoints plugin hooks",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh ${CLAUDE_PLUGIN_ROOT:-${CHECKPOINTS_ROOT}}/hooks/strip-attribution.sh"
          }
        ]
      }
    ]
  }
}
